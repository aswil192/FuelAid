<!DOCTYPE html>
<html lang="en">

<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title>Delivery Fleet | RoadAid Admin</title>

	<!-- Fonts -->
	<link href="https://fonts.googleapis.com/css?family=Poppins:300,400,500,600,700,800&display=swap" rel="stylesheet">

	<!-- CSS -->
	<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
	<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css">
	<link rel="stylesheet" href="/assets/css/dashboard-redesign.css">
	<link rel="icon" type="image/png" href="/assets/images/FUEL AID_logo.png">

	<style>
		body {
			background-color: var(--primary-bg);
			color: var(--text-light);
			font-family: var(--font-main);
		}

		#main-wrapper {
			margin-left: 280px;
			padding: 2.5rem;
			min-height: 100vh;
		}

		.page-header {
			margin-bottom: 3rem;
			border-left: 5px solid var(--accent-yellow);
			padding-left: 1.5rem;
			display: flex;
			justify-content: space-between;
			align-items: center;
		}

		.page-header h2 {
			font-weight: 800;
			margin: 0;
			text-transform: uppercase;
			letter-spacing: 1px;
		}

		.count-badge-hero {
			background: #111;
			padding: 0.5rem 1.5rem;
			border-radius: 50px;
			border: 1px solid #333;
			display: flex;
			align-items: center;
			gap: 1rem;
		}

		.count-badge-hero span:first-child {
			color: var(--accent-yellow);
			font-weight: 800;
			font-size: 1.25rem;
		}

		.count-badge-hero span:last-child {
			color: #888;
			font-size: 0.8rem;
			text-transform: uppercase;
			font-weight: 700;
		}

		.delivery-grid {
			display: grid;
			grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
			gap: 2rem;
		}

		.delivery-card-premium {
			background: var(--card-bg);
			border-radius: 25px;
			overflow: hidden;
			color: var(--text-dark);
			border: 1px solid #333;
			transition: var(--transition);
			display: flex;
			flex-direction: column;
		}

		.delivery-card-premium:hover {
			transform: translateY(-5px);
			box-shadow: 0 15px 40px rgba(0, 0, 0, 0.4);
		}

		.card-visual-header {
			height: 40px;
			background: #111;
			position: relative;
		}

		.verified-stamp {
			position: absolute;
			top: 15px;
			right: 15px;
			background: var(--accent-yellow);
			color: #000;
			padding: 0.25rem 0.75rem;
			border-radius: 50px;
			font-size: 0.65rem;
			font-weight: 800;
			text-transform: uppercase;
		}

		.delivery-profile-overlap {
			padding: 0 2rem;
			margin-top: -20px;
			margin-bottom: 1.5rem;
			display: flex;
			align-items: flex-end;
			gap: 1rem;
		}

		.delivery-avatar-box {
			width: 70px;
			height: 70px;
			background: #fff;
			border-radius: 15px;
			display: flex;
			align-items: center;
			justify-content: center;
			font-size: 1.5rem;
			font-weight: 800;
			color: #111;
			box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
			border: 4px solid var(--card-bg);
		}

		.delivery-meta h5 {
			margin: 0;
			font-weight: 800;
			color: #111;
			font-size: 1.1rem;
		}

		.delivery-meta p {
			margin: 0;
			color: #777;
			font-size: 0.8rem;
			font-weight: 600;
		}

		.delivery-body-details {
			padding: 0 2rem 2rem;
			flex-grow: 1;
		}

		.info-strip {
			display: flex;
			align-items: center;
			gap: 1rem;
			margin-bottom: 1rem;
		}

		.info-strip i {
			width: 30px;
			color: var(--accent-yellow);
			font-size: 1rem;
		}

		.info-strip span {
			font-size: 0.85rem;
			font-weight: 600;
			color: #333;
		}

		.delivery-action-footer {
			padding: 1.5rem 2rem;
			background: #f8f9fa;
		}

		.btn-view-profile {
			display: block;
			width: 100%;
			padding: 0.9rem;
			background: #111;
			color: #fff;
			text-align: center;
			text-decoration: none;
			border-radius: 12px;
			font-weight: 700;
			font-size: 0.85rem;
			text-transform: uppercase;
			transition: var(--transition);
		}

		.btn-view-profile:hover {
			background: var(--accent-yellow);
			color: #000;
			transform: scale(1.02);
		}

		@media (max-width: 991px) {
			#main-wrapper {
				margin-left: 0;
				padding: 1.5rem;
			}
		}
	</style>
</head>

<body class="dashboard-body">
	<%- include('../partials/header') %>
		<%- include('../partials/adminSidebar', { title: 'Fuel Delivery Boys' }) %>

			<div id="main-wrapper">
				<div class="page-header">
					<div>
						<h2>Delivery <span>Fleet</span></h2>
						<p class="text-muted mb-0">Monitor your verified fuel delivery service providers.</p>
					</div>
					<div class="count-badge-hero">
						<span>
							<%= fueldeliveryboys.length %>
						</span>
						<span>Active Operators</span>
					</div>
				</div>

				<%- include('../partials/alerts') %>

					<% if(fueldeliveryboys.length===0) { %>
						<div class="text-center py-5"
							style="background: rgba(255,255,255,0.02); border-radius: 30px; border: 2px dashed #333;">
							<i class="fas fa-truck-pickup mb-4" style="font-size: 4rem; color: #444;"></i>
							<h3 class="text-muted">No Operators Found</h3>
							<p class="text-muted">Verified fuel delivery providers will be listed here.</p>
						</div>
						<% } else { %>
							<div class="delivery-grid">
								<% fueldeliveryboys.forEach((boy, index)=> { %>
									<div class="delivery-card-premium">
										<div class="card-visual-header">
											<span class="verified-stamp"><i
													class="fas fa-check-circle me-1"></i>Verified</span>
										</div>

										<div class="delivery-profile-overlap">
											<div class="delivery-avatar-box">
												<%= boy.firstName.charAt(0) %>
													<%= boy.lastName.charAt(0) %>
											</div>
											<div class="delivery-meta">
												<h5>
													<%= boy.firstName %>
														<%= boy.lastName %>
												</h5>
												<p>Operator ID: #<%= boy._id.toString().slice(-6) %>
												</p>
											</div>
										</div>

										<div class="delivery-body-details">
											<div class="info-strip">
												<i class="fas fa-envelope"></i>
												<span>
													<%= boy.email %>
												</span>
											</div>
											<div class="info-strip">
												<i class="fas fa-phone"></i>
												<span>
													<%= boy.phone || 'N/A' %>
												</span>
											</div>
											<div class="info-strip">
												<i class="fas fa-map-marker-alt"></i>
												<span>
													<%= boy.address || 'N/A' %>
												</span>
											</div>
										</div>

										<div class="delivery-action-footer">
											<a href="/admin/delivery/view/<%= boy._id %>" class="btn-view-profile">
												Full Profile Details <i class="fas fa-arrow-right ms-2"></i>
											</a>
										</div>
									</div>
									<% }) %>
							</div>
							<% } %>
			</div>

			<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>